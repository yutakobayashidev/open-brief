# OpenBrief Mobile

Flutter companion for the OpenBrief daemon. It presents the finite Brief and Return Thread, streams Agent updates, and confirms triage proposals.

```console
flutter pub get
flutter run
flutter analyze
flutter test
```

The app requires an HTTPS OpenBrief remote API URL and device token. Plain HTTP is accepted only for loopback development.
