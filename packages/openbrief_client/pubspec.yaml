name: openbrief_client
description: Typed Dart client for the OpenBrief remote API.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.2

dependencies:
  http: ^1.6.0
  web_socket_channel: ^3.0.3

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.3
